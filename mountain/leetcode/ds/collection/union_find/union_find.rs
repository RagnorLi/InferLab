/**
 * 并查集 (Union-Find / Disjoint Set) - Rust 实现
 * 
 * ## 需求说明
 * 
 * 实现一个并查集数据结构，要求：
 * 
 * ### 核心特性
 * - 用于管理元素的分组，支持集合的合并与查询
 * 
 * ### 必须实现的操作
 * 1. `new(n)` - 创建并查集，n个元素各自独立
 * 2. `find(x)` - 查找x所在集合的代表元素
 * 3. `union(x, y)` - 合并x和y所在的集合
 * 4. `connected(x, y)` - 判断x和y是否在同一集合
 * 5. `count()` - 返回集合数量
 * 
 * ### 优化技术
 * - 路径压缩
 * - 按秩合并
 * 
 * ### 时间复杂度要求
 * - 所有操作：O(α(n)) 近似O(1)
 */
