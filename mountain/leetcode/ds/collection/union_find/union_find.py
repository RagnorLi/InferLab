"""
并查集 (Union-Find / Disjoint Set) - Python 实现

## 需求说明

实现一个并查集数据结构，要求：

### 核心特性
- 用于管理元素的分组，支持集合的合并与查询
- 高效判断两个元素是否在同一集合
- 支持路径压缩和按秩合并优化

### 必须实现的操作
1. `__init__(n)` - 初始化，n个元素各自独立成集合
2. `find(x)` - 查找x所在集合的代表元素 O(α(n)) 近似O(1)
3. `union(x, y)` - 合并x和y所在的集合 O(α(n))
4. `connected(x, y)` - 判断x和y是否在同一集合 O(α(n))
5. `count()` - 返回集合数量 O(1)

### 优化技术
- 路径压缩：find时直接将路径上所有节点指向根节点
- 按秩合并：总是将秩小的树合并到秩大的树下

### 应用场景
- 连通性问题
- 最小生成树（Kruskal算法）
- 图的连通分量
- 朋友圈问题

### 时间复杂度要求
- 所有操作：O(α(n))，α为阿克曼函数的反函数，近似O(1)

"""
