/**
 * 哈希集合 (HashSet) - TypeScript 实现
 * 
 * ## 需求说明
 * 
 * 实现一个哈希集合数据结构，要求：
 * 
 * ### 核心特性
 * - 无序元素集合，基于哈希表实现
 * - 元素唯一
 * 
 * ### 必须实现的操作
 * 1. `add(value)` - 添加元素 O(1) 平均
 * 2. `delete(value)` - 删除元素
 * 3. `has(value)` - 判断元素是否存在
 * 4. `union(other)` - 并集操作
 * 5. `intersection(other)` - 交集操作
 * 6. `size()` - 返回元素数量
 * 
 * ### 时间复杂度要求
 * - 插入/查找/删除：O(1) 平均
 */
