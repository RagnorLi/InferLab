"""
哈希表 (Hash Table) - Python 实现

## 需求说明

实现一个哈希表数据结构，要求：

### 核心特性
- 基于哈希函数的键值映射
- 平均时间复杂度 O(1) 的查找、插入、删除
- 需要处理哈希冲突

### 必须实现的操作
1. `__init__(capacity)` - 初始化，指定初始容量
2. `hash(key)` - 哈希函数，将键映射到索引
3. `insert(key, value)` - 插入键值对 O(1) 平均
4. `get(key)` - 获取值 O(1) 平均
5. `delete(key)` - 删除键值对 O(1) 平均
6. `contains(key)` - 判断键是否存在 O(1) 平均
7. `resize()` - 扩容（负载因子过高时）

### 冲突处理方式
- 开放寻址法（线性探测、二次探测、双重哈希）
- 链地址法（链表数组）

### 哈希函数设计
- 除法哈希法：hash(key) = key % capacity
- 乘法哈希法
- 字符串哈希（如 djb2, sdbm）

### 时间复杂度要求
- 插入/查找/删除：O(1) 平均，O(n) 最坏

"""
