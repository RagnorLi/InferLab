"""
位集合 (BitSet) - Python 实现

## 需求说明

实现一个位集合数据结构，要求：

### 核心特性
- 用位向量表示集合
- 每个位表示一个元素是否存在
- 空间效率高，适合表示小范围的整数集合

### 必须实现的操作
1. `__init__(size)` - 初始化，指定位集合大小
2. `set(index)` - 设置第index位为1 O(1)
3. `clear(index)` - 设置第index位为0 O(1)
4. `flip(index)` - 翻转第index位 O(1)
5. `get(index)` - 获取第index位的值 O(1)
6. `count()` - 统计1的个数 O(n) 或 O(1)（维护计数器）
7. `all()` - 判断是否所有位都是1
8. `any()` - 判断是否有任何位是1
9. `none()` - 判断是否所有位都是0
10. `union(other)` - 位或运算
11. `intersection(other)` - 位与运算
12. `difference(other)` - 位差运算

### 应用场景
- 布尔数组的压缩表示
- 快速集合运算
- 位掩码
- 布隆过滤器

### 时间复杂度要求
- 单个位操作：O(1)
- 统计操作：O(n) 或 O(1)

"""
