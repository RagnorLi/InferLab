/**
 * 位集合 (BitSet) - C++ 实现
 * 
 * ## 需求说明
 * 
 * 实现一个位集合数据结构，要求：
 * 
 * ### 核心特性
 * - 用位向量表示集合
 * - 每个位表示一个元素是否存在
 * 
 * ### 必须实现的操作
 * 1. `BitSet(size)` - 构造函数，指定大小
 * 2. `set(index)` - 设置第index位为1
 * 3. `reset(index)` - 设置第index位为0
 * 4. `flip(index)` - 翻转第index位
 * 5. `test(index)` - 获取第index位的值
 * 6. `count()` - 统计1的个数
 * 7. `all()` - 判断是否所有位都是1
 * 8. `any()` - 判断是否有任何位是1
 * 9. `none()` - 判断是否所有位都是0
 * 
 * ### 时间复杂度要求
 * - 单个位操作：O(1)
 */
