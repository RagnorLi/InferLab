/**
 * 位集合 (BitSet) - Rust 实现
 * 
 * ## 需求说明
 * 
 * 实现一个位集合数据结构，要求：
 * 
 * ### 核心特性
 * - 用位向量表示集合
 * - 每个位表示一个元素是否存在
 * 
 * ### 必须实现的操作
 * 1. `new(size)` - 创建位集合，指定大小
 * 2. `set(index)` - 设置第index位为1
 * 3. `clear(index)` - 设置第index位为0
 * 4. `get(index)` - 获取第index位的值，返回 bool
 * 5. `count_ones()` - 统计1的个数
 * 6. `all()` - 判断是否所有位都是1
 * 7. `any()` - 判断是否有任何位是1
 * 
 * ### 时间复杂度要求
 * - 单个位操作：O(1)
 */
