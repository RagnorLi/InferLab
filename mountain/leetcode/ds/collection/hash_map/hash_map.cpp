/**
 * 哈希映射 (HashMap) - C++ 实现
 * 
 * ## 需求说明
 * 
 * 实现一个哈希映射数据结构，要求：
 * 
 * ### 核心特性
 * - 键值对集合，基于哈希表实现
 * - 键唯一，值可以重复
 * 
 * ### 必须实现的操作
 * 1. `put(key, value)` - 添加或更新键值对
 * 2. `get(key)` - 获取值
 * 3. `remove(key)` - 删除键值对
 * 4. `containsKey(key)` - 判断键是否存在
 * 5. `keys()` - 获取所有键
 * 6. `size()` - 返回键值对数量
 * 
 * ### 时间复杂度要求
 * - 插入/查找/删除：O(1) 平均
 */
