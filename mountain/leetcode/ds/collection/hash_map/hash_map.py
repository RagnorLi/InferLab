"""
哈希映射 (HashMap) - Python 实现

## 需求说明

实现一个哈希映射数据结构，要求：

### 核心特性
- 键值对集合，基于哈希表实现
- 键唯一，值可以重复
- 无序存储

### 必须实现的操作
1. `put(key, value)` - 添加或更新键值对 O(1) 平均
2. `get(key)` - 获取值 O(1) 平均
3. `remove(key)` - 删除键值对 O(1) 平均
4. `contains_key(key)` - 判断键是否存在 O(1) 平均
5. `keys()` - 获取所有键 O(n)
6. `values()` - 获取所有值 O(n)
7. `items()` - 获取所有键值对 O(n)
8. `size()` - 返回键值对数量 O(1)
9. `clear()` - 清空所有键值对 O(n)

### 与哈希表的区别
- HashMap 是 HashTable 的更高级抽象
- 提供更多便利方法
- 可能支持 null 键和值

"""
