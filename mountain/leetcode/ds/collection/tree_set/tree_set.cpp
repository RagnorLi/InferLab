/**
 * 树集合 (TreeSet) - C++ 实现
 * 
 * ## 需求说明
 * 
 * 实现一个树集合数据结构，要求：
 * 
 * ### 核心特性
 * - 基于平衡二叉搜索树的有序元素集合
 * - 元素唯一，有序存储
 * 
 * ### 必须实现的操作
 * 1. `insert(value)` - 添加元素 O(log n)
 * 2. `erase(value)` - 删除元素 O(log n)
 * 3. `find(value)` - 查找元素 O(log n)
 * 4. `begin()` - 获取最小元素
 * 5. `end()` - 获取最大元素
 * 6. `lower_bound(value)` - 获取大于等于value的最小元素
 * 7. `upper_bound(value)` - 获取大于value的最小元素
 * 
 * ### 时间复杂度要求
 * - 所有操作：O(log n)
 */
