/**
 * 树集合 (TreeSet) - TypeScript 实现
 * 
 * ## 需求说明
 * 
 * 实现一个树集合数据结构，要求：
 * 
 * ### 核心特性
 * - 基于平衡二叉搜索树的有序元素集合
 * - 元素唯一
 * 
 * ### 必须实现的操作
 * 1. `add(value)` - 添加元素 O(log n)
 * 2. `delete(value)` - 删除元素 O(log n)
 * 3. `has(value)` - 判断元素是否存在 O(log n)
 * 4. `first()` - 获取最小元素
 * 5. `last()` - 获取最大元素
 * 6. `rangeQuery(start, end)` - 范围查询
 * 
 * ### 时间复杂度要求
 * - 所有操作：O(log n)
 */
