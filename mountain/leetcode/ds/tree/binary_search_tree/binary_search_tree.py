"""
二叉搜索树 (Binary Search Tree) - Python 实现

## 需求说明

实现一个二叉搜索树数据结构，要求：

### 核心特性
- 左子树所有节点的值 < 根节点值
- 右子树所有节点的值 > 根节点值
- 支持高效的查找、插入、删除操作

### 必须实现的操作
1. `insert(value)` - 插入节点 O(log n) 平均，O(n) 最坏
2. `search(value)` - 查找节点 O(log n) 平均，O(n) 最坏
3. `delete(value)` - 删除节点 O(log n) 平均，O(n) 最坏
4. `min()` - 查找最小值 O(log n)
5. `max()` - 查找最大值 O(log n)
6. `inorder()` - 中序遍历（有序输出）O(n)
7. `predecessor(value)` - 查找前驱节点
8. `successor(value)` - 查找后继节点

### 删除节点的三种情况
- 无子节点：直接删除
- 一个子节点：用子节点替代
- 两个子节点：用后继节点替代

### 时间复杂度要求
- 查找/插入/删除：O(log n) 平均，O(n) 最坏（退化为链表）
- 遍历：O(n)

"""
