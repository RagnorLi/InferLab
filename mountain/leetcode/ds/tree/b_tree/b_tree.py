"""
B 树 (B-Tree) - Python 实现

## 需求说明

实现一个 B 树数据结构，要求：

### 核心特性
- 多路平衡搜索树
- 每个节点可以有多个子节点（通常大于2）
- 适用于磁盘存储，减少IO次数
- 所有叶子节点在同一层

### 关键参数
- 阶数（order）m：每个节点最多有 m 个子节点
- 每个节点最多有 m-1 个关键字
- 非根节点至少有 ⌈m/2⌉ 个子节点

### 必须实现的操作
1. `insert(key)` - 插入关键字 O(log n)
2. `search(key)` - 查找关键字 O(log n)
3. `delete(key)` - 删除关键字 O(log n)
4. `split(node, index)` - 节点分裂
5. `merge(node, index)` - 节点合并

### 应用场景
- 数据库索引（MySQL InnoDB）
- 文件系统
- 需要大量磁盘IO的场景

### 时间复杂度要求
- 查找/插入/删除：O(log n)
- 每次操作最多访问 O(log n) 个节点

"""
