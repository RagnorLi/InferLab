/**
 * B 树 (B-Tree) - C++ 实现
 * 
 * ## 需求说明
 * 
 * 实现一个 B 树数据结构，要求：
 * 
 * ### 核心特性
 * - 多路平衡搜索树
 * - 每个节点可以有多个子节点
 * - 适用于磁盘存储
 * 
 * ### 关键参数
 * - 阶数（order）m：每个节点最多有 m 个子节点
 * - 每个节点最多有 m-1 个关键字
 * 
 * ### 必须实现的操作
 * 1. `insert(key)` - 插入关键字 O(log n)
 * 2. `search(key)` - 查找关键字 O(log n)
 * 3. `remove(key)` - 删除关键字 O(log n)
 * 4. `split(node)` - 节点分裂
 * 5. `merge(node)` - 节点合并
 * 
 * ### 时间复杂度要求
 * - 所有操作：O(log n)
 */
