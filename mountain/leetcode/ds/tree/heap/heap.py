"""
堆 (Heap) - Python 实现

## 需求说明

实现一个堆数据结构，要求：

### 核心特性
- 完全二叉树
- 父节点值 >= 子节点值（大顶堆）或 <=（小顶堆）
- 通常用数组存储

### 必须实现的操作
1. `__init__(is_max=True)` - 初始化，is_max 决定大顶堆或小顶堆
2. `insert(value)` - 插入元素 O(log n)
3. `extract()` - 提取并删除堆顶元素 O(log n)
4. `peek()` - 查看堆顶元素但不删除 O(1)
5. `heapify()` - 堆化操作 O(log n)
6. `build_heap(array)` - 从数组构建堆 O(n)
7. `size()` - 返回堆大小 O(1)
8. `is_empty()` - 判断是否为空 O(1)

### 数组索引关系
- 父节点：i
- 左子节点：2*i + 1
- 右子节点：2*i + 2

### 时间复杂度要求
- 插入：O(log n)
- 删除：O(log n)
- 构建堆：O(n)
- 查找堆顶：O(1)

"""
