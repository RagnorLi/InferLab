/**
 * 字符串 (String) - Rust 实现
 * 
 * ## 需求说明
 * 
 * 实现一个字符串数据结构，要求：
 * 
 * ### 核心特性
 * - 字符序列，基于字符数组
 * - 支持字符串的基本操作
 * 
 * ### 必须实现的操作
 * 1. `new()` - 创建新字符串
 * 2. `from(str)` - 从字符串字面量创建
 * 3. `len()` - 返回字符串长度（字节数）O(1)
 * 4. `chars()` - 返回字符迭代器
 * 5. `push(ch)` - 追加字符 O(1) 平均
 * 6. `push_str(s)` - 追加字符串 O(n)
 * 7. `substring(start, end)` - 获取子串 O(n)
 * 8. `find(pattern)` - 查找子串位置 O(n*m)
 * 9. `replace(from, to)` - 替换子串 O(n)
 * 10. `split(delimiter)` - 分割字符串
 * 
 * ### 实现方式
 * - 可以使用 Vec<u8> 或 Vec<char>
 * - 考虑 UTF-8 编码
 * 
 * ### 时间复杂度要求
 * - 随机访问：O(1) 字节级别
 * - 拼接/替换：O(n)
 */
