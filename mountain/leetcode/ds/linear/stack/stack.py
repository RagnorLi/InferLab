"""
栈 (Stack) - Python 实现

## 需求说明

实现一个栈数据结构，要求：

### 核心特性
- LIFO（Last In First Out）后进先出
- 只能从栈顶进行插入和删除操作

### 必须实现的操作
1. `__init__()` - 初始化空栈
2. `push(value)` - 压入元素到栈顶 O(1)
3. `pop()` - 弹出并返回栈顶元素 O(1)
4. `peek()` - 返回栈顶元素但不删除 O(1)
5. `is_empty()` - 判断栈是否为空 O(1)
6. `__len__()` - 返回栈的大小 O(1)

### 实现方式
- 可以用数组/列表实现
- 也可以用链表实现

### 时间复杂度要求
- 入栈：O(1)
- 出栈：O(1)
- 查看栈顶：O(1)

"""
