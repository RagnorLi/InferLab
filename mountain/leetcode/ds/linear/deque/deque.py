"""
双端队列 (Deque) - Python 实现

## 需求说明

实现一个双端队列数据结构，要求：

### 核心特性
- 可以在队列的两端进行插入和删除操作
- 同时具有栈和队列的特性

### 必须实现的操作
1. `__init__()` - 初始化空双端队列
2. `append_left(value)` - 在左端添加元素 O(1)
3. `append_right(value)` - 在右端添加元素 O(1)
4. `pop_left()` - 从左端删除并返回元素 O(1)
5. `pop_right()` - 从右端删除并返回元素 O(1)
6. `peek_left()` - 返回左端元素但不删除 O(1)
7. `peek_right()` - 返回右端元素但不删除 O(1)
8. `is_empty()` - 判断是否为空 O(1)
9. `__len__()` - 返回大小 O(1)

### 实现方式
- 可以用双向链表实现
- 也可以用循环数组实现

### 时间复杂度要求
- 两端插入/删除：O(1)
- 查看两端元素：O(1)

"""
