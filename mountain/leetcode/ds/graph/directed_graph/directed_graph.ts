/**
 * 有向图 (Directed Graph) - TypeScript 实现
 * 
 * ## 需求说明
 * 
 * 实现一个有向图数据结构，要求：
 * 
 * ### 核心特性
 * - 边有方向，u->v 和 v->u 是不同的边
 * 
 * ### 必须实现的操作
 * 1. `addVertex(vertex)` - 添加顶点
 * 2. `addEdge(u, v)` - 添加有向边
 * 3. `getInDegree(vertex)` - 获取入度
 * 4. `getOutDegree(vertex)` - 获取出度
 * 5. `topologicalSort()` - 拓扑排序
 */
