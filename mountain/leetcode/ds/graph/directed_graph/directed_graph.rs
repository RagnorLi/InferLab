/**
 * 有向图 (Directed Graph) - Rust 实现
 * 
 * ## 需求说明
 * 
 * 实现一个有向图数据结构，要求：
 * 
 * ### 核心特性
 * - 边有方向，u->v 和 v->u 是不同的边
 * 
 * ### 必须实现的操作
 * 1. `add_vertex(vertex)` - 添加顶点
 * 2. `add_edge(u, v)` - 添加有向边
 * 3. `get_in_degree(vertex)` - 获取入度
 * 4. `get_out_degree(vertex)` - 获取出度
 * 5. `topological_sort()` - 拓扑排序
 */
