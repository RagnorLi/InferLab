/**
 * 无向图 (Undirected Graph) - C++ 实现
 * 
 * ## 需求说明
 * 
 * 实现一个无向图数据结构，要求：
 * 
 * ### 核心特性
 * - 边没有方向，连接是双向的
 * - (u, v) 和 (v, u) 表示同一条边
 * 
 * ### 必须实现的操作
 * 1. `addVertex(vertex)` - 添加顶点
 * 2. `addEdge(u, v)` - 添加无向边
 * 3. `removeEdge(u, v)` - 删除无向边
 * 4. `getDegree(vertex)` - 获取顶点的度
 * 5. `bfs(start)` - 广度优先搜索
 * 6. `dfs(start)` - 深度优先搜索
 */
