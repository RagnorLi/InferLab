"""
图（邻接矩阵）(Graph - Adjacency Matrix) - Python 实现

## 需求说明

实现一个基于邻接矩阵的图数据结构，要求：

### 核心特性
- 用二维数组表示图
- matrix[i][j] 表示顶点 i 到顶点 j 是否有边
- 适合稠密图（边数接近顶点数平方）

### 必须实现的操作
1. `__init__(num_vertices)` - 初始化，指定顶点数
2. `add_edge(u, v)` - 添加边 O(1)
3. `remove_edge(u, v)` - 删除边 O(1)
4. `has_edge(u, v)` - 判断是否有边 O(1)
5. `get_neighbors(vertex)` - 获取邻接顶点列表 O(V)
6. `get_vertices()` - 获取所有顶点 O(V)

### 存储方式
- 使用二维数组（列表的列表）
- 无向图：对称矩阵
- 有向图：非对称矩阵

### 应用场景
- 稠密图
- 需要快速判断边是否存在
- 小规模图

### 空间复杂度
- O(V²)

"""
